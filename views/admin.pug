extends layout

block content
    h1= title
    hr
    div(id="pin-page" class="page")
        p Toto je zoznam aktuálne aktívnych PINov.
        table(id="active-pins")
            tr
                th PIN
                th Platnosť do
            tr
                td 111222
                td 1122334
        label(for='newPin') PIN:
        input#newPin(type='text', placeholder='12345' name='newPin' maxlength=10)
        button(id="saveNewPin") Pridať aktívny PIN
        span(id="addPinStatus" class="statusMsg")
    hr
    div(id="hint-page" class="page")
        p Tu môžete modifikovať nápovedy pre 13. záhradu
        textarea(id="newHintsText" rows="20" cols="80" placeholder="Nové nápovedy vložte sem a kliknite Uložiť.")
        button(id="saveHints") Uložiť nové nápovedy
        span(id="saveHintsStatus" class="statusMsg")
        script.
            initAdmin();