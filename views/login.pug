extends layout

block content
    h1= title
    p Vitajte v nápovedách pre 13. záhradu
    div(id='login-page')
        label(for='pin') PIN:
        input(id="pin" type='text', placeholder='12345' name='PIN')
        button(id="doLogin") Otvoriť nápovedu
        span(id="loginStatus" class="statusMsg")
        p Pokiaľ nemáte PIN, tak kontaktujte správcov 13. záhrady.
    script.
        initLogin();
